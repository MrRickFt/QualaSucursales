import { FiltroSucursalesPipe } from './filtro-sucursales.pipe';

describe('FiltroSucursalesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroSucursalesPipe();
    expect(pipe).toBeTruthy();
  });
});
